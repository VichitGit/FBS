<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vichitpov.com.fbs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_fbs_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_fbs_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.StartLoginActivity" />
        <activity android:name=".ui.activity.LoginDifferentActivity" />
        <activity android:name=".ui.activity.RegisterUserActivity" />
        <activity android:name=".ui.activity.VerificationCodeActivity" />
        <activity android:name=".ui.activity.PostToSellActivity" />
        <activity android:name=".ui.activity.MainCategoryActivity" />
        <activity android:name=".ui.activity.UserProfileActivity" />
        <activity android:name=".ui.activity.PostToBuyActivity" />
        <activity android:name=".ui.activity.BookmarkCategoriesActivity"></activity>
    </application>

</manifest>